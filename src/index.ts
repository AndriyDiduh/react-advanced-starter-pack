import * as React from 'react'
import * as ReactDOM from 'react-dom'
import AppGenerate from './app_generate'

// render Generated APP frontend
ReactDOM.render(React.createElement(AppGenerate), document.getElementById('root'))
